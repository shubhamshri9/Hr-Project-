HR Gate Pass Management System - Phase 2 UI/UX

1. Copy your existing data/ and uploads/ folders if you already have them.
2. Replace app.py with this Phase 2 app.py.
3. Run:
   pip install -r requirements.txt
   streamlit run app.py

This version improves the UI/UX while retaining the existing Excel/database workflow.
